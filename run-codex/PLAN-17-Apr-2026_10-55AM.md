# PLAN — Claude Help Files for Profile10-amargupta.tech

## Objective
Personal portfolio site at amargupta.tech. Next.js 16 + Three.js/GSAP + Supabase-backed content. Root CLAUDE.md is nearly empty — full rewrite + add slash commands, subagents, hooks, per-area nested CLAUDE.md, and create settings.

## Current State
- Root: `/Volumes/maersk/amargupta/Documents/Latest Projects/Portfolio Project/Profile10-amargupta.tech`
- `CLAUDE.md` — only `@AGENTS.md`.
- `AGENTS.md` — standard reminder.
- No `.claude/` directory.
- Structure: `src/{app,components,lib}`, `supabase/migrations/`, `public/`.
- Tech: Next.js 16.2.3, React 19, Tailwind v4, Three.js + drei + fiber + postprocessing, GSAP, Framer Motion, Lenis, `@supabase/ssr`, `next-themes`, `react-icons`, `@radix-ui/react-slot`, `tailwind-merge`, `clsx`, `class-variance-authority`.
- Supabase is used for storage (images via `/storage/v1/object/public/**` domain in `next.config.ts`) and likely for content (projects, writing, etc.).
- Has `supabase/migrations/` — verify pattern inside and preserve conventions.

## Target State
```
CLAUDE.md                            # full rewrite
AGENTS.md                            # (leave)
.claude/
  settings.local.json                # create with hooks + permissions
  commands/
    new-section.md
    add-3d-scene.md
    add-migration.md
    add-project-entry.md
    audit-accessibility.md
    perf-check.md
  agents/
    three-fiber-reviewer.md
    gsap-lenis-reviewer.md
    supabase-content-reviewer.md
    migration-reviewer.md
  hooks/
    block-env-commit.sh
    prevent-tailwind-config.sh
    lint-on-write.sh
src/app/CLAUDE.md
src/components/CLAUDE.md
src/lib/CLAUDE.md
supabase/CLAUDE.md
```

## Step-by-step Instructions

### Step 1 — Full rewrite of `CLAUDE.md`
```markdown
@AGENTS.md

# amargupta.tech — Personal Portfolio

## Overview
Personal portfolio + writing site. Next.js 16 frontend, heavy 3D/motion, Supabase as a lightweight content store (projects, writing, assets via Supabase Storage).

## Nested Context
- `src/app/` — routes + metadata conventions
- `src/components/` — section / three / motion / ui
- `src/lib/` — helpers + Supabase clients
- `supabase/` — migrations + storage buckets

## Tech Stack
- Next.js 16.2.3 (App Router, Turbopack), React 19, TypeScript strict
- Tailwind CSS v4 — CSS-first via `@theme` in `src/app/globals.css`. NO `tailwind.config.js`.
- `next-themes` for dark mode
- Three.js 0.183 + `@react-three/fiber` 9 + `@react-three/drei` 10 + `@react-three/postprocessing` 3
- GSAP 3.15 + `@gsap/react` 2
- Framer Motion 12, Lenis 1.3
- `@supabase/ssr` + `@supabase/supabase-js` (SSR-aware client)

## Commands
```
npm run dev     # Dev server
npm run build
npm run lint
```

## Env
```
NEXT_PUBLIC_SUPABASE_URL=...
NEXT_PUBLIC_SUPABASE_ANON_KEY=...
SUPABASE_SERVICE_ROLE_KEY=...
```

## Conventions
- Server Components by default.
- 3D scenes: client-only, lazy-loaded via `next/dynamic { ssr: false }`, memoize geometries.
- GSAP: `useGSAP({ scope })` from `@gsap/react`.
- Respect `prefers-reduced-motion` everywhere.
- Images: `next/image`. Remote hosts already whitelisted in `next.config.ts` — add to that list if you add a new host.
- Supabase Storage URLs use the public bucket pattern configured in `next.config.ts`.

## Rules
- No `tailwind.config.*` — Tailwind v4.
- Service-role Supabase key stays server-only. Never import into a client component.
- Never commit `.env*` files.
```

### Step 2 — `.claude/commands/`

All concise, similar style. Key ones:

**`new-section.md`** — scaffold a marketing section that pulls data from Supabase when needed (server component + SSR client), with metadata-friendly exports.

**`add-3d-scene.md`** — scaffold a new R3F scene with `'use client'`, dynamic import, Suspense, and reduced-motion guard.

**`add-migration.md`** — create next `NNN_snake_case.sql` in `supabase/migrations/` with RLS + `auth.uid()` policies.

**`add-project-entry.md`** — `$ARGUMENTS` is the project slug. Insert a new row into the `projects` (or equivalent) table via SQL helper and upload assets to the correct Supabase Storage bucket. If the table doesn't exist, stop and report.

**`audit-accessibility.md`** — WCAG audit of the diff.

**`perf-check.md`** — `npm run build`, parse Next output, flag non-lazy R3F imports.

Use the same frontmatter format shown in the ai-chat-devfrend plan.

### Step 3 — `.claude/agents/`

**`three-fiber-reviewer.md`**, **`gsap-lenis-reviewer.md`** — same as Devfrend Agency Portfolio plan.

**`supabase-content-reviewer.md`:**
```markdown
---
name: supabase-content-reviewer
description: Audits Supabase usage for client selection and safe image URLs.
tools: Read, Grep, Glob
---

Checklist:
1. Service-role client never imported from a client component.
2. Server components fetching Supabase data use the SSR-aware client from `@supabase/ssr`.
3. All public image URLs go through `next/image`, using hosts whitelisted in `next.config.ts`.
4. No `SUPABASE_SERVICE_ROLE_KEY` leaked into a `NEXT_PUBLIC_` variable or client bundle.

Report file:line violations. Do NOT modify.
```

**`migration-reviewer.md`** — same structure as Memory MCP plan.

### Step 4 — `.claude/hooks/`
`block-env-commit.sh`, `prevent-tailwind-config.sh`, `lint-on-write.sh` — identical implementations as other projects.

### Step 5 — Per-area CLAUDE.md

**`src/app/CLAUDE.md`** — App Router + metadata + `next-themes` provider placement; Lenis mounted at root.

**`src/components/CLAUDE.md`** — folders `sections/`, `three/`, `motion/`, `ui/`. R3F dynamic-imported. GSAP with `useGSAP`.

**`src/lib/CLAUDE.md`** — helpers + Supabase clients (SSR client, browser client, service-role — each in its own file, the service-role one imports `'server-only'`).

**`supabase/CLAUDE.md`** — migrations pattern (`NNN_snake_case.sql`, RLS + ownership), Storage bucket conventions (public buckets for portfolio assets, private for drafts).

### Step 6 — Create `.claude/settings.local.json`
```json
{
  "permissions": {
    "allow": [
      "Bash(npm run dev)",
      "Bash(npm run build)",
      "Bash(npm run lint)",
      "Bash(npm run start)",
      "Bash(npx tsc --noEmit*)",
      "Bash(npx eslint *)",
      "Bash(git status)",
      "Bash(git diff*)",
      "Bash(git log*)",
      "Bash(git add *)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      { "matcher": "Bash", "hooks": [{ "type": "command", "command": "bash .claude/hooks/block-env-commit.sh" }] },
      { "matcher": "Write", "hooks": [{ "type": "command", "command": "bash .claude/hooks/prevent-tailwind-config.sh" }] }
    ],
    "PostToolUse": [
      { "matcher": "Edit|Write", "hooks": [{ "type": "command", "command": "bash .claude/hooks/lint-on-write.sh" }] }
    ]
  }
}
```

### Step 7
```
chmod +x .claude/hooks/*.sh
```

## DO NOTs
- DO NOT modify source in `src/`, `public/`, `supabase/migrations/`, `next.config.ts`, `package.json`, `tsconfig.json`, `eslint.config.mjs`, `postcss.config.mjs`.
- DO NOT create `tailwind.config.*`.
- DO NOT add dependencies.
- DO NOT commit.

## Verification
```
ls .claude/commands .claude/agents .claude/hooks
ls src/app/CLAUDE.md src/components/CLAUDE.md src/lib/CLAUDE.md supabase/CLAUDE.md
grep -q "amargupta.tech" CLAUDE.md && echo "root rewritten"
node -e "JSON.parse(require('fs').readFileSync('.claude/settings.local.json','utf8')); console.log('ok')"
```
