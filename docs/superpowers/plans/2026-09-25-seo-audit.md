# Plan — lane 3: is amargupta.tech SEO-proof? Measure, then fix what the measurements name

Asked by Amar, 2026-09-25: *"check the application are seo proff"*. Context spec:
`../kamai/docs/superpowers/specs/2026-09-25-blog-to-google-design.md`. The build loop is global Rule 0
(`/build-loop`). Leader: session `kamai-8e`. Gate commands: this repo's `CLAUDE.md` / `docs/DOC-MAP.md`.

**Files this lane must NOT edit** (lane 1 owns them): `src/app/sitemap.ts`, `src/lib/queries.ts`,
`src/app/rss.xml/`, `src/app/llms.txt/`, `public/llms.txt`. Everything else in the repo is yours; if a
finding lives in a lane-1 file, write it in the audit and message the leader.

## Baseline already measured (live post page, 2026-09-25)
Present: `<title>`, meta description, canonical, `og:title`, `og:type=article`, `twitter:card=
summary_large_image`, `article:published_time`, `robots: index, follow`, one `<h1>`, 4 JSON-LD blocks incl.
`Article` (its `image` = the site-wide `/opengraph-image`). **Missing: `og:image` on the post page.**
`robots.txt` is `Allow: /` with the sitemap listed. Home responded in 1.9 s on one curl.

## Phase 1 — audit, measured (no fixes yet)
- For `/`, `/about`, `/projects`, `/blog`, one project page and two blog posts: Lighthouse SEO +
  Performance (mobile and desktop) in headless Chrome, the page's JSON-LD against schema.org `Article` /
  `BlogPosting` required and recommended fields, `og:*` and `twitter:*` completeness, heading order, image
  `alt`, internal links to each post, status codes for a missing slug (404, not 200), and trailing-slash /
  `www` redirects (one canonical host).
- Create `docs/seo-audit-2026-09-25.md`: every finding with the measured value, the page, and severity.
  Commit it; send the leader the top findings. Nothing about "SEO-proof" is claimed from a checklist that
  was not measured.

## Phase 2 — fix the top findings, test first
- Per-post Open Graph image (`src/app/blog/[slug]/opengraph-image.tsx`, the Next file convention), so
  `og:image` and the JSON-LD `image` are the post's own. Then the other high-severity findings from phase 1,
  one commit each, each with a test that failed first.
- Checkpoint: gate green, commit on your branch, message the leader with the SHAs. **Push only your branch; the leader integrates into main and deploys** (a main push deploys amargupta.tech, and it goes out only on Amar's OK). After deploy: re-run phase 1's measurements
  on production and append before/after numbers to the audit file.
